# Invio Prodotti al Server

Questo script permette di inserire manualmente titolo, prezzo e descrizione di un prodotto,
validare i dati e inviarli al server tramite una richiesta POST.

## Uso
1. Installare le dipendenze:
   ```bash
   pip install -r requirements.txt